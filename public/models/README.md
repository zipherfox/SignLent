# Model Files

Place your model files here:

- `labels.json` — Label list (e.g. `{ "labels": ["hello", "thank_you", "yes", ...] }`)
- `config.json` — ML config with normalization params, thresholds, and model shape
- `model.tflite` — TensorFlow Lite model file

These are loaded automatically when the app starts.
